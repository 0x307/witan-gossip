/target
/_legacy
pqc-gossip/dist/

# cargo-component regenerates this on every `cargo component build`. The crate
# does not use it — guest bindings come from wit_bindgen::generate! in
# src/component.rs — so it is a build artifact, not source.
pqc-gossip/src/bindings.rs
